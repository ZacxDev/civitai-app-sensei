node_modules
dist
*.local

# Local env files — never commit these (only `.env.example` is tracked).
.env
.env.development
.env.production
.env*.local

.DS_Store

# Local screenshot scratch (curated shots live in docs/screenshots/).
.shots
