# The dev harness (`npm run dev:harness`) fires a fake BLOCK_INIT from
# window.location.origin; the SDK transport drops mismatched-origin messages, so
# the harness origin must be allowlisted. Set to the dev server origin.
VITE_BLOCK_ALLOWED_PARENT_ORIGINS=http://localhost:5189
